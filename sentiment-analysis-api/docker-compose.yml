version: '3.8'
services:
  sentiment-api:
    build: .
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
      FLASK_ENV: production
      CORS_ORIGINS: http://localhost:5000
      RATE_LIMIT: 120 per minute
      DATABASE_PATH: /tmp/sentiment_history.db
    restart: unless-stopped
